# Requires Python >= 3.11
# Note: For Windows users, it is highly recommended to install spatial libraries 
# (like geopandas and shapely) via Conda to avoid C-extension compilation errors.
# Command: conda install --file requirements.txt -c conda-forge -y

pandas>=2.0.0
numpy>=1.24.0
networkx>=3.0
geopandas>=0.14.0
shapely>=2.0.0
streamlit>=1.30.0
pydeck>=0.8.0
plotly>=5.18.0
scikit-learn>=1.3.0
requests>=2.31.0
python-dotenv>=1.0.0
